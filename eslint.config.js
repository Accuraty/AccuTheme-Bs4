export default [
	// ...other config
	{
		// Note: there should be no other properties in this object
		ignores: ["**/node_modules/", "**/dist/"],
	},
];

  // was .eslintignore
  // **/node_modules/
  // **/dist/
